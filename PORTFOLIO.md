# 🍱 반찬 유통기한 관리 앱 — 프로젝트 포트폴리오

**기간:** 2025년  
**역할:** 기획 · 디자인 · 개발 (개인 프로젝트)  
**링크:** [GitHub Repository](https://github.com/ksh1283f/food-storage)

---

## 왜 만들었나요?

냉장고를 열 때마다 "이거 언제 만든 거지?" 하는 상황이 반복됐습니다. 반찬을 만든 날짜를 기억하지 못해 멀쩡한 음식을 버리거나, 반대로 상한 반찬을 먹게 되는 경험이 계기가 됐습니다.

단순한 메모 앱이 아닌, **카테고리별 권장 보관일을 자동 계산**하고 **AI가 남은 재료로 레시피를 추천**해주는 앱을 만들었습니다.

---

## 어떤 앱인가요?

반찬을 등록하면 카테고리와 보관 방법(냉장/냉동)에 따라 유통기한이 자동으로 계산됩니다. 홈 화면에서 D-Day를 한눈에 확인하고, 레시피 탭에서는 보유 재료를 기반으로 AI가 만들 수 있는 요리를 추천해줍니다.

**핵심 사용 흐름**
1. 반찬 이름 + 카테고리 선택 → 유통기한 자동 설정
2. 홈에서 D-Day 확인 → 색상으로 위험도 파악
3. 레시피 탭 → AI 추천 요리 확인 → 상세 레시피 열람
4. 매일 오전 9시 알림으로 먹어야 할 반찬 리마인더

---

## 기술 스택

- **React Native + Expo** — 크로스 플랫폼 앱 개발
- **expo-router** — 파일 기반 라우팅 (Next.js 스타일)
- **TypeScript** — 타입 안정성
- **Zustand** — 전역 상태 관리
- **Firebase Auth** — 이메일 기반 인증
- **AsyncStorage** — 로컬 데이터 영속화
- **expo-notifications** — 로컬 푸시 알림
- **REST API 연동** — AI 레시피 추천 백엔드

### 백엔드 (직접 구축)

- **Node.js + Express 5 + TypeScript** — REST API 서버
- **PostgreSQL** — 레시피 저장 및 조회
- **Firebase Admin SDK** — JWT 토큰 검증 (서버사이드 인증)
- **OpenAI GPT-4o-mini** — 재료 기반 레시피 추천 생성
- **Render** — 서버 및 DB 배포

---

## AI 활용

### GPT-4o-mini 기반 레시피 추천

단순 키워드 검색이 아닌, **현재 보유 재료와 곧 상하는 재료를 분석해 맞춤 레시피를 생성**하는 기능을 구현했습니다.

**설계 포인트**

- 곧 상하는 재료를 우선적으로 소진하도록 프롬프트에 명시적으로 지시
- 부족한 재료(`missingIngredients`)를 함께 반환해 사용자가 장을 볼 때 참고할 수 있도록 구성
- 출력 형식을 JSON으로 고정해 파싱 오류 없이 안정적으로 데이터를 처리
- 한국 가정식 위주, 난이도 조절 가능하도록 요구사항을 프롬프트에 반영

**요청 예시**

```json
{
  "ingredients": ["달걀", "두부", "대파", "간장"],
  "expiringIngredients": ["두부"],
  "preferences": { "difficulty": "easy" }
}
```

**응답 구조**

```json
{
  "recipes": [
    {
      "name": "두부조림",
      "description": "...",
      "ingredients": ["두부", "간장", "대파"],
      "missingIngredients": [],
      "steps": ["..."],
      "score": 0.95
    }
  ]
}
```

---

## 개발 도구

이 프로젝트는 **Claude (Anthropic)** 를 개발 보조 도구로 활용했습니다. 코드 설계, 디버깅, 배포 문제 해결 전반에 걸쳐 Claude와 협업하며 개발 속도를 높이고 더 나은 구조를 만들었습니다.

---

## 어려웠던 점과 해결 방법

### 📌 Firebase 세션이 앱 재시작 시 초기화되는 문제

**상황**  
로그인 후 앱을 종료하고 다시 열면 로그인 화면으로 돌아가는 문제가 발생했습니다. Firebase의 `initializeAuth` + `getReactNativePersistence`로 세션을 유지하려 했지만, `@react-native-async-storage` v2와 Firebase 12 사이의 호환 문제로 로그인 요청 자체가 응답 없이 멈추는 현상이 나타났습니다.

**분석 과정**  
Metro 번들러가 `firebase/auth`를 웹 번들로 해석해 `getReactNativePersistence` 함수가 런타임에서 정의되지 않거나, AsyncStorage의 초기화 과정에서 Promise가 resolve되지 않아 Firebase 인증 초기화 자체가 block되는 것을 확인했습니다.

**해결**  
Firebase 내장 persistence 대신, 로그인 성공 시 자격증명을 AsyncStorage에 저장하고 앱 시작 시 자동 재로그인하는 방식으로 우회했습니다. `onAuthStateChanged`가 null을 반환할 때 저장된 세션으로 백그라운드 로그인을 시도해 UX를 해치지 않도록 구현했습니다.

```
앱 시작 → onAuthStateChanged(null)
  → 즉시 loading=false (로그인 화면 표시)
  → 백그라운드에서 AsyncStorage 확인
  → 세션 있으면 자동 로그인 → 탭 화면으로 이동
```

---

### 📌 API 인증 토큰 중복 처리 문제

**상황**  
Firebase ID 토큰을 모든 API 요청 헤더에 첨부해야 했는데, 화면마다 토큰을 가져오는 코드가 반복되었습니다.

**해결**  
공통 HTTP 클라이언트(`api.ts`)를 구현해 모든 요청에서 Firebase ID 토큰을 자동으로 헤더에 첨부하도록 추상화했습니다. 각 화면에서는 인증 로직 없이 비즈니스 로직에만 집중할 수 있게 됐습니다.

---

### 📌 서버 시작 시 Firebase 초기화 실패로 배포 헬스체크 타임아웃

**상황**
Firebase 서비스 계정 환경변수가 설정되지 않은 상태에서 서버를 배포하니 Render 헬스체크가 계속 타임아웃되며 배포에 실패했습니다.

**분석 과정**
`firebase.ts`가 모듈 로드 시점에 `throw`를 호출하도록 구현되어 있어, 환경변수가 없으면 서버가 포트에 바인딩되기도 전에 프로세스가 종료되는 것을 확인했습니다.

**해결**
초기화 실패를 `throw` 대신 `initError` 변수에 저장하고, 실제 인증이 필요한 요청이 들어올 때 503을 반환하도록 변경했습니다. 서버는 항상 정상 기동되어 헬스체크를 통과하고, 인증 오류는 요청 단계에서 명확하게 처리됩니다.

---

### 📌 레시피 추천 시 OpenAI API 중복 호출 문제

**상황**
레시피 추천 탭에 진입할 때마다 OpenAI API를 호출하다 보니 응답 속도가 느리고 API 비용이 불필요하게 발생했습니다.

**해결**
PostgreSQL에 `recipes` 테이블을 구축해 한 번 생성된 레시피를 저장하도록 변경했습니다. 동일한 재료 조합에 대한 재요청 시 DB에서 바로 반환해 응답 속도를 개선하고 API 호출 비용을 줄였습니다.

---

### 📌 로컬 개발 환경에서 TypeScript 커스텀 타입 선언이 적용 안 되는 문제

**상황**
`express.d.ts`로 `req.user` 타입을 확장했음에도 `ts-node`로 실행 시 타입 에러가 발생했습니다.

**해결**
`ts-node`는 기본적으로 `tsconfig.json`의 `include` 목록을 무시하는데, `--files` 플래그를 추가하면 포함된 모든 파일(타입 선언 포함)을 함께 로드합니다.

---

## 배운 점

- **Metro 번들러의 모듈 해석 방식** — `react-native` 필드와 conditional exports가 어떻게 동작하는지 직접 디버깅하며 이해했습니다.
- **Firebase SDK 내부 동작** — 인증 초기화 순서, persistence 메커니즘, `onAuthStateChanged` 타이밍을 깊이 학습했습니다.
- **Zustand 패턴** — 단순한 전역 상태부터 비동기 액션, 스토어 간 의존성 처리까지 실제 앱에 적용했습니다.
- **expo-router** — 파일 기반 라우팅과 인증 가드 패턴(AuthGate)을 구현하며 Next.js와 유사한 구조를 경험했습니다.
- **서버사이드 인증 설계** — 프론트에서 발급한 Firebase ID 토큰을 서버에서 검증하는 흐름을 직접 구현하며 JWT 기반 인증의 전체 사이클을 이해했습니다.
- **배포 환경 디버깅** — 로컬에서 동작하던 코드가 배포 환경에서 실패하는 원인(모듈 초기화 타이밍, 환경변수 누락)을 분석하고 해결했습니다.
- **프롬프트 엔지니어링** — GPT-4o-mini가 JSON만 반환하도록 시스템 프롬프트를 설계하고, 재료 우선순위(곧 상하는 재료 우선)를 반영한 추천 로직을 구현했습니다.

---

## 스크린샷

> *(스크린샷을 여기에 추가하세요)*

| 홈 화면 | 반찬 추가 | 레시피 추천 | 레시피 상세 |
|---------|----------|------------|------------|
| ![홈]() | ![추가]() | ![레시피]() | ![상세]() |
